---
description: Lexsys monorepo layout — where source lives and where to edit (links to docs/ for contracts)
globs:
alwaysApply: true
---
# Lexsys Project Directory Structure

Registry-first React UI monorepo (pnpm + Turborepo).

```txt
packages/registry → packages/cli → consumer project (user-owned code)
```

**Contracts and rules:** [docs/INDEX.md](../../docs/INDEX.md) — not duplicated here.  
**Agent routing:** [AGENTS.md](../../AGENTS.md).

Public APIs: `package.json` `exports` only. Never deep-import another package's `src/` or `dist/`.

---

## Repository root

```txt
lexsys/
├── .agents/            # Agent workspace — skills/ (tracked)
├── .cursor/rules/      # Cursor editing behavior (*.mdc)
├── apps/               # playground (smoke), docs (placeholder)
├── docs/               # Architecture and domain specs — source of truth
├── packages/           # tokens, ui, registry, cli
├── AGENTS.md           # Always-loaded agent routing
├── CONTRIBUTING.md
└── package.json        # Root turbo / pnpm scripts
```

**Ignore for source work:** `node_modules/`, `dist/`, `.turbo/`, `.pnpm-store/`, `.tmp/`

---

## Packages (where to edit)

| Package | Path | Output |
| ------- | ---- | ------ |
| Tokens | `packages/tokens/src/` | `dist/` + generated CSS |
| UI reference | `packages/ui/src/components/{primitives,blocks,templates}/` | `dist/` |
| Registry | `packages/registry/src/items/` + `templates/` | `dist/` + bundled templates |
| CLI | `packages/cli/src/` | `dist/` (`lexsys` bin) |

| App | Path | Role |
| --- | ---- | ---- |
| Playground | `apps/playground/` | Maintenance-only monorepo smoke — [TESTING.md](../../docs/TESTING.md#verification-surfaces) |
| Docs site | `apps/docs/` | Placeholder |

---

## Where to change what

| Task | Primary location | Doc |
| ---- | ---------------- | --- |
| Token values / layers | `packages/tokens/src/` | [TOKENS.md](../../docs/TOKENS.md) |
| Component behavior | `packages/ui/src/components/…` | [UI.md](../../docs/UI.md), [UI_COMPOSITION.md](../../docs/UI_COMPOSITION.md) |
| Install metadata / templates | `packages/registry/` | [REGISTRY.md](../../docs/REGISTRY.md) |
| CLI install behavior | `packages/cli/src/` | [CLI.md](../../docs/CLI.md) |
| After UI edits → templates | run `pnpm registry:sync` | `$registry-sync` skill |
| Verification | sandbox + `pnpm *:check` | [TESTING.md](../../docs/TESTING.md) |
| Script names | — | [SCRIPTS.md](../../docs/SCRIPTS.md) only |

Component file shape (all layers):

```txt
ComponentName/
├── ComponentName.tsx
├── ComponentName.types.ts
└── ComponentName.variants.ts
```

---

## Cursor rules (`.cursor/rules/`)

| Rule | Purpose |
| ---- | ------- |
| `cursor-rules.mdc` | How to add/edit rules |
| `project-structure.mdc` | This file — layout and edit map |
| `docs-routing.mdc` | Doc ownership when editing `docs/**` |
| `documentation-standards.mdc` | Doc governance (`docs/`, README, CONTRIBUTING) |
| `code-commenting.mdc` | JSDoc and comments in `src/` |
| `git-commit-rules.mdc` | Meta: authoring commit rules |
| `lexsys-testing.mdc` | Vitest layout and commands per package |
| `tech-stack.mdc` | Dependencies and toolchain |
| `self-improvement.mdc` | When to evolve rules |

Repo skills: [`.agents/skills/`](../../.agents/skills/). Git commit/PR policy: [`$git-commit`](../../.agents/skills/git-commit/SKILL.md) → [git-commits.mdc](../../.agents/skills/git-commit/git-commits.mdc).
