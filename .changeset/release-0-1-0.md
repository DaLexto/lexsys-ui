---
"@dalexto/lexsys": minor
---

Stable MVP release `0.1.0` on npm dist-tag `latest`: docs site scaffold, admin catalog expansion, component token coverage for remaining primitives, remote registry checksum verification and allowlist support, and `lexsys status --json`.
