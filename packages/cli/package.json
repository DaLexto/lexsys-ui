{
  "name": "neurex-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "neurex-ui": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured yet\"",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@neurex-ui/registry": "workspace:*"
  }
}