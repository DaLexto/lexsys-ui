# @dalexto/lexsys-docs

Minimal static documentation shell for Lexsys (Vite). Canonical specs remain in
the monorepo `docs/` tree; this app provides a quickstart page and catalog links.

```bash
pnpm docs:dev
pnpm docs:build
```
